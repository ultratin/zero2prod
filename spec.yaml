name: zero2prod
region: sfo
services:
  - name: zero2prod
    dockerfile_path: dockerfile
    source_dir: .
    github:
      branch: main
      repo: ultratin/zero2prod
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
    routes:
      - path: /
databases:
  - engine: PG
    name: newsletter
    num_nodes: 1
    size: db-s-dev-database
    version: "12"